server {
    listen 10000;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — react-router owns client-side routes like /inbox,
    # /tickets, etc., which don't exist as files.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
